# 更新日志

<!-- RELEASE-NOTES:START -->
## 0.5.0 - 2026-08-19

### GUI 完整管理

- 桌面界面新增订阅添加和删除，删除前会明确提示本地文章也将一并移除
- 资源库、摘录与想法、归档文章和资料库管理改为主界面全宽页面，不再使用漂浮的一级窗口
- 资源编辑改为右侧检查器，可直接查看和修改用途、备注、隐私与评分
- 修复全文搜索与二级窗口叠加、页面切换残留遮罩、弹窗按钮无法点击等交互问题

### DeepSeek AI

- 资料库管理中可安全保存、测试和删除 DeepSeek API Key
- 支持为 RSS 文章生成中文总结与翻译，并将结果保存在本地数据库
- 资源库可补全网站用途、适用场景、能力、限制、分类、标签、价格、登录要求和语言
- 兼容 DeepSeek 返回的 Markdown JSON、缺省可选字段和不同 evidence 结构，失败提示会给出具体解析原因

### 资源记忆库

- 资源库页面支持添加网站、导入网页收藏、搜索、归档、编辑和重新补全
- AI 补全信息直接显示在右侧编辑栏；缺失时会显示原因并提供立即补全入口
- CLI 与 GUI 继续共享同一份本地资源数据库和搜索索引

## 0.3.0 - 2026-08-17

### 个人资料库

- 全文搜索升级为 SQLite FTS5 trigram 索引和 BM25 相关性排序，保留中文一至二字短词的精确匹配
- 搜索结果继续高亮关键词，并新增去重的最近搜索历史、结果数与一键清空
- 摘录保存字符偏移及前后文稳定锚点；正文更新后会重新定位重复文本中的正确位置
- 新增文章标签和独立的“稍后读”队列
- 文章列表支持批量勾选、批量收藏、批量加入稍后读和批量归档

### 阅读完整度

- 数学公式使用内嵌 MathJax 排版为 SVG，后台生成且失败时保留 TeX 源文本
- 响应式图片不再把 `<picture>` 内多个候选重复显示，并保留图片说明文字
- 新增定义列表，以及带 `rowspan` / `colspan` 的复杂表格语义与逻辑列布局
- 新增真实结构 fixture、SVG 视觉快照和 HTML5 DOM / Readability 候选对比测试
- 忽略元素清理改为 HTML5 DOM 节点删除，并单独保留 `math/tex` 公式节点
- 标题、`base href` 和 `picture/srcset` 改为 DOM 属性读取与节点归一，不再依赖手写标签边界
- 新增 Rust Blog 无 `article` 完整页和 Simon Willison 畸形正文 fixture，保持现有语义 Block 与渲染接口不变

### 离线与数据治理

- 远程图片首次成功加载后写入本地 SHA-256 内容寻址缓存；相同图片只存一份，后续阅读可直接离线显示
- 图片缓存默认限制 1 GB 并按最近使用自动淘汰；资料库管理中可查看占用、收缩到 512 MB 或完整清空
- 新增 SQLite 完整性与外键检查、在线一致性备份、恢复和 `VACUUM` 压缩；恢复前强制创建安全副本
- 新增普通备份和 Windows 当前用户 DPAPI 加密备份，备份默认保留最近 10 份
- 左栏新增“资料库管理”，集中显示数据库、图片缓存、备份与日志空间，并提供明确的清理策略和操作反馈
<!-- RELEASE-NOTES:END -->

## 0.2.1 - 2026-08-13

### 新增

- 订阅文章正文标题下新增“收藏文章 / 已收藏”按钮，收藏后可在左栏“文章收藏”中随时阅读
- 收藏、网页保存与文字摘录使用不同名称和反馈，入口及保存位置更清楚

### 修复

- 修复 Martin Fowler Architecture 等指南页、索引页保存后正文卡片可能被截断的问题
- 保留卡片标题的层级和链接，收藏后仍可点击前往对应文章
- 修复网页标题重复、行内链接拆词、多行列表断开，以及列表内链接、加粗和图片丢失的问题
- 收藏或取消收藏只有在数据库写入成功后才更新界面；失败会显示原因

## 0.2.0 - 2026-08-13

### 新增

- 左栏新增统一“收藏”入口，可集中阅读普通星标文章与本地网页快照
- 支持粘贴网页地址抓取正文，也支持直接粘贴完整 HTML 或 HTML 片段
- 网页正文保存到本机 SQLite，重启应用或原网页失效后仍可阅读已保存文字
- 自动提取网页标题和正文，过滤脚本、导航、页头页尾等非阅读内容
- 支持 GBK 等旧中文编码、重定向、相对链接与网页声明的基础地址
- 本地网页可继续使用跨段选择、摘录和写想法，并支持确认后永久删除

### 交互与安全

- 网页抓取在后台进行，不阻塞阅读界面；可取消等待，并提供明确成功或失败反馈
- 下载只接受 HTTP/HTTPS HTML，连接超时 10 秒、总超时 45 秒，解压后正文上限为 8 MiB
- 网页及正文图片会拒绝本机、内网地址和不安全的重定向目标
- 重复收藏同一网址会保留为独立快照，不覆盖旧正文与已有摘录
- 当前版本保存正文 HTML；网页中的远程图片仍需联网加载

## 0.1.1 - 2026-08-13

### 修复

- Windows 正式版双击启动时不再出现黑色命令行窗口
- 从正文打开网页或图片时不再短暂闪现命令行窗口

### 调整

- 桌面程序与命令行工具拆分为 `shiyue.exe` 和 `shiyue-cli.exe`，保留原有命令行功能与输出

## 0.1.0 - 2026-08-13

拾阅首次公开发布。

### 新增

- 三栏式 RSS 订阅、文章列表与正文阅读界面
- 中文文章排版、引用、代码块、列表与图文顺序渲染
- 图片懒加载、四任务并发限制、失败自动重试与明确错误反馈
- 跨段落文字选择、复制、收藏和记录想法
- “摘录与想法”资料库
- 文章归档以及恢复归档
- 后台定时更新、失败退避、系统通知与托盘驻留

### 兼容性

- 正式名称改为“拾阅”，发行程序名改为 `shiyue.exe`
- 继续读取早期 `rrss` 数据目录，原有订阅、归档和摘录无需迁移
